# پذیرش فاز ۱ — ۱۲ بند، چگونه نشان می‌دهیم، صورتجلسه

بندها همان ۱۲ بندِ چک‌لیستِ تحویل‌شده به مشتری (`docs/client/04-checklist-paziresh-faz-1.md`) هستند — متن بندها را تغییر ندهید؛ ستون «چگونه نشان داده می‌شود» برای اجراکنندهٴ جلسه است. جلسه زنده روی **سامانهٴ واقعی** (production) با حساب‌های سازمان دمو یا مدرسهٴ واقعی برگزار می‌شود؛ ترتیب و زمان‌بندی در `docs/demo/phase-1-script.md`. هر بند «بله» یا «خیر»؛ «خیر» با توضیح در «موارد باز».

| # | چه چیزی نشان داده می‌شود | چگونه نشان داده می‌شود (اجراکننده) | بله | خیر |
|---|---|---|---|---|
| ۱ | سامانه با آدرس اینترنتی امن (قفل سبز مرورگر) روی گوشی و کامپیوتر باز می‌شود | `https://<PUBLIC_HOST>` روی لپ‌تاپ (Chrome) و گوشی مدیر؛ کلیک روی قفل → «اتصال ایمن»؛ `http://` خودکار به `https://` می‌رود. پشت‌صحنه: `curl -I` هدر HSTS، `openssl s_client` تاریخ گواهی > ۶۰ روز | ☐ | ☐ |
| ۲ | یک دانش‌آموز با موبایل یا نام‌کاربری و رمز وارد می‌شود و صفحهٴ خانهٴ او کارهای امروز و نزدیکش را نشان می‌دهد | گوشی B: ورود سارا محمدی (`09123000005`؛ دانش‌آموزِ بدون موبایل با نام‌کاربری `کدمدرسه-شمارهٴدانش‌آموزی` مثل `g-14050021`) → خانه: بنر مدرسه با سلام + نوار «امروز» (سررسیده/امروز/خوانده‌نشده) + کاشی‌های «کارهای من» / «انجام‌شده» / «کلاس من» (کلاس ۱۰/۲) + کارت «کارهای نزدیک». تأکید: با کیبورد فارسی و ارقام فارسی هم وارد می‌شود | ☐ | ☐ |
| ۳ | یک دبیر وارد می‌شود و صفحهٴ خانهٴ او با صفحهٴ دانش‌آموز کاملاً متفاوت است و «کلاس‌های من» را نشان می‌دهد | گوشی A: ورود علی کریمی (`09123000003`) → خانه: کاشی‌های «کلاس‌های من» / «کارهایی که دادم» / «کار جدید»؛ «کلاس‌های من» → صفحهٴ کلاس‌ها (۱۰/۱ و ۱۰/۲ ریاضی با تعداد دانش‌آموز و کارهای باز)؛ کارت «کارهای نزدیک» با نوار پیشرفت، دکمهٴ «کار جدید». دو گوشی را کنار هم نگه دارید | ☐ | ☐ |
| ۴ | در اولین ورود با رمز اولیه، سامانه تغییر رمز را اجباری می‌کند | با دانش‌آموزی که در بند ۷ ساخته شد (رمز اولیه از برگه): ورود → مستقیم `/change-password`؛ تلاش برای رفتن به `/inbox` یا `/home` → دوباره همان صفحه؛ رمزِ برابرِ شماره یا «12345678» رد می‌شود؛ رمز درست → خانه. (پشت‌صحنه: `curl` اکشن با این نشست → `PASSWORD_CHANGE_REQUIRED`) | ☐ | ☐ |
| ۵ | دبیر یک «کار» با عنوان، توضیح و مهلت برای یک کلاس ثبت می‌کند و در گوشی دانش‌آموز اعلان و کار جدید دیده می‌شود | گوشی A: «کار جدید» → نوع تکلیف، عنوان «حل تمرین‌های صفحهٴ ۴۲»، توضیح، مهلت (تاریخ جلالی فردا ۲۰:۰۰)، گیرنده: کلاس ۱۰/۲ → ثبت («۲۱ نفر»). گوشی B (بدون رفرش دستی): نشان «پنل من» و اعلان تا ≤ ۴۵ ثانیه بالا می‌رود؛ باز کردن → جزئیات کار | ☐ | ☐ |
| ۶ | دانش‌آموز روی کار کامنت می‌گذارد و وضعیت آن را «انجام‌شده» می‌کند و دبیر همان لحظه تغییر را می‌بیند | گوشی B: نظر «تمرین ۳ را متوجه نشدم» + «انجام شد». گوشی A: صفحهٴ کار → پیشرفت «۱/۲۱ انجام شد» و نظر؛ اعلان «نظر جدید». تأکید: دانش‌آموز نمی‌تواند کار را حذف یا برای دیگران تغییر دهد | ☐ | ☐ |
| ۷ | مدیر سامانه (ادمین) بدون کمک توسعه‌دهنده یک کلاس، یک دانش‌آموز و یک دبیر تازه می‌سازد | لپ‌تاپ: ورود مریم رضایی (`09123000002`، مدیر دبیرستان دخترانه) → «مدیریت» → کلاس‌ها → «کلاس جدید» ۱۰/۳ → دانش‌آموزان → «دانش‌آموز جدید» (نام، شمارهٴ دانش‌آموزی، موبایل، کلاس ۱۰/۳؛ رمز اولیه یک‌بار نمایش) → کارکنان → «همکار جدید» (دبیر جدید) → ارائهٴ درس ریاضی ۱۰/۳ به دبیر جدید. مدیر خودش کلیک می‌کند، شما فقط می‌گویید کجا | ☐ | ☐ |
| ۸ | مدیر سامانه رمز یک کاربر را ریست می‌کند و حساب قفل‌شده را باز می‌کند | قبل از جلسه یک حساب دمو را با ۶ رمز غلط قفل کنید (یا `status='locked'` در SQL). لپ‌تاپ: پروندهٴ فرد → «رفع قفل» → ورود موفق. سپس «تعیین رمز موقت» روی سارا → رمز جدید نمایش → گوشی B خودکار خارج شده (نشست باطل) → ورود با رمز موقت → تغییر رمز اجباری | ☐ | ☐ |
| ۹ | ورود اطلاعات از فایل اکسل نمونه انجام شده و اجرای دوبارهٴ همان فایل هیچ تغییر یا رکورد تکراری ایجاد نمی‌کند | لپ‌تاپ (ترمینال، قبل از جلسه یک‌بار commit شده): `pnpm tsx scripts/import.ts <file>.xlsx --org <slug> --school <code> --as <موبایل مدیر> --dry-run` → جدول خلاصه ۰ خطا؛ `--commit` دوباره روی همان فایل → `inserted: — (total 0)`. در پنل: شمارندهٴ دانش‌آموزان تغییر نکرده (`/admin/onboarding`) | ☐ | ☐ |
| ۱۰ | برگهٴ چاپی نام‌کاربری و رمز برای دانش‌آموزان و دبیران یک کلاس نمونه چاپ شده و در دسترس است | برگه‌های A4 چاپ‌شدهٴ کلاس ۱۰/۲ (`/admin/classes/<id>/credentials`، ۳×۴ برگهٴ بریدنی) در پاکت روی میز؛ یک برگه را نشان دهید: نام مدرسه، نام، کلاس، شناسهٴ ورود، رمز اولیه، «در اولین ورود رمز را تغییر دهید». لپ‌تاپ: همان صفحه با «چاپ» (پیش‌نمایش چاپ) | ☐ | ☐ |
| ۱۱ | سامانه روی گوشی اندروید و آیفون به‌صورت اپلیکیشن نصب می‌شود (بدون نیاز به استور) و بعد از خروج از حساب، صفحه‌های خصوصی از حافظهٴ گوشی نمایش داده نمی‌شوند | اندروید (Chrome): منو → «افزودن به صفحهٴ اصلی» / بنر نصب → آیکون روی هوم → باز شدن تمام‌صفحه. آیفون (Safari): اشتراک‌گذاری → «Add to Home Screen». سپس در اپ نصب‌شده: «بیشتر» → «خروج» → دکمهٴ برگشت / باز کردن دوبارهٴ اپ → صفحهٴ ورود، نه «پنل من» (`Clear-Site-Data` + `no-store`). حالت آفلاین: صفحهٴ «اتصال نیست» (نه دادهٴ قدیمی) | ☐ | ☐ |
| ۱۲ | نسخهٴ پشتیبان رمزگذاری‌شده گرفته شده و یک‌بار بازیابی آن با موفقیت تمرین شده است | لپ‌تاپ (ترمینال ssh): `ls -la /srv/school/backups/` → `db-<دیروز>.dump.age` و `LAST_OK` امروز؛ `rclone lsf arvan:school-backups/<host>/` → همان فایل در باکت؛ `cat backups/LAST_RESTORE_TEST` → سطر `OK person=… work_item=… elapsed=…s`؛ جدول «نتیجهٴ تمرین بازیابی» در `deploy/README.md` با تاریخ و امضا. نشان دهید که فایل بدون کلید age (که در پاکت است) باز نمی‌شود: `age -d db-….dump.age` → خطا | ☐ | ☐ |

## در فاز ۱ نیست (اگر پرسیده شد)

تکالیف ساختارمند، دفتر کلاسی و حضور‌وغیاب، نمرات و اعتراض به نمره، پیام‌رسان و کانال کلاس، پیوست فایل به کار، اعلان پوش/پیامک، ولی/سرپرست، امور مالی، آپلود اکسل از داخل پنل (فعلاً CLI توسط توسعه‌دهنده)، حالت تیره. همه در `/roadmap` با ماه تحویل دیده می‌شوند و در الحاقیه (`docs/client/05-elhaghieh-faz-1.md`) توافق شده‌اند.

---

## صورتجلسهٴ تحویل فاز ۱

**تاریخ:** ..................................  **ساعت:** ............  **محل / لینک:** ..................................

**مدرسه / سازمان:** ..................................  **نشانی سامانه:** `https://` ..................................

**نسخهٴ تحویل‌شده:** ایمیج `edu-app:` ............ (خروجی `/api/health` → `version`)  **تگ گیت:** `phase1-freeze`

**افراد حاضر:**

| نام و نام خانوادگی | سمت | امضا |
|---|---|---|
|  | مدیر مدرسه |  |
|  | معاون / نمایندهٴ فنی مدرسه |  |
|  | توسعه‌دهنده |  |

**نتیجهٴ بندها:** بله: ...... از ۱۲ · خیر: ...... (شماره‌ها: ............)

**نتیجهٴ کلی جلسه:** ☐ تأیید و پذیرش فاز ۱ &nbsp;&nbsp; ☐ پذیرش مشروط (موارد باز پیوست شود) &nbsp;&nbsp; ☐ عدم پذیرش

**موارد باز و مهلت رفع:**

| # بند | شرح | مسئول | مهلت |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

**تحویل‌داده‌ها:** ☐ پاکت مهرشدهٴ کلید بازیابی بکاپ (age) ☐ پاکت برگه‌های اعتبارنامهٴ کلاس(های) ............ ☐ رمز مدیر سازمان (یک‌بار، حضوری) ☐ اطلاعیهٴ حریم خصوصی (`docs/client/06`) ☐ کانال پشتیبانی (نام گروه/شماره): ............

**توضیحات:**

..................................................................

..................................................................

**امضای نمایندهٴ مدرسه:** .................................. &nbsp;&nbsp;&nbsp; **امضای توسعه‌دهنده:** ..................................
