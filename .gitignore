# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
/build
/dist

# misc
.DS_Store
Thumbs.db
*.pem
*.log

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit secrets; the example is the only allowed one
.env*
!.env.example

# local uploads / data
/uploads
/data

# typescript
*.tsbuildinfo
next-env.d.ts
.claude/

# import CLI: pre-commit dumps and credential CSVs (plaintext!) — never commit
/backups
