// Drizzle tables of the `tenancy` PostgreSQL schema. Definitions live in src/db/schema/tenancy.ts
// (drizzle-kit reads them from there); module code imports from here.
export {
  academicYear,
  branch,
  classGroup,
  classOffering,
  educationLevel,
  gradeLevel,
  organization,
  school,
  subject,
  term,
} from "@/db/schema/tenancy";
