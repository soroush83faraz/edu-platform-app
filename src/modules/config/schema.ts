// Drizzle tables of the `config` PostgreSQL schema. Definitions live in src/db/schema/config.ts
// (drizzle-kit reads them from there); module code imports from here.
export {
  featureFlag,
  settingValue,
} from "@/db/schema/config";
