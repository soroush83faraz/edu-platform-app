// Drizzle tables of the `audit` PostgreSQL schema. Definitions live in src/db/schema/audit.ts
// (drizzle-kit reads them from there); module code imports from here.
export {
  auditLog,
} from "@/db/schema/audit";
